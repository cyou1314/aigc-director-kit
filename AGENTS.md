# CineThread 工作规则

## 项目边界

- 这是一个公开、可复现的纯 Python 合同与选择器库。
- 不提交私人素材、角色图、视频、Blender 工程、API key、Cookie 或本地绝对路径。
- 不把技术验证冒充成最终 AIGC 成片质量，也不自动触发付费生成。
- 动作目录只描述可选动作，不内置来源不清或许可证未核验的动捕数据。

## 开发规则

- 运行时只使用 Python 标准库；保持安装成本低。
- 合同校验要确定性、可读、可定位到字段；未知扩展字段默认保留兼容性。
- 自然语言动作请求只能选择目录中已有动作；未知动作必须明确拒绝，不得编造。
- 修改行为时同步更新示例、测试和 CHANGELOG。
- 优先使用 `python -m unittest discover -s tests -v` 验证。

## 版本边界

- `0.1.x` 只承诺镜头计划校验、交接提示、动作目录检索和动作请求编译。
- Blender、MCP、视频抽帧、云端生成和成片 QC 都通过后续适配器接入，不成为核心运行时依赖。
